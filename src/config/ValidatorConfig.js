const messages = {
  required: (field) => `qqq`,
  email: (field) => `Niepoprawny format adresu e-mail`
}

const locale = {
  name: 'pl',
  messages,
  attributes: {}
}

export default locale
